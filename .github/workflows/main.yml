name: QaGuru

on:
  push:
    branches: [ master ]
  pull_request:
    branches: [ master ]

# выполняемые действия
jobs:
  build-and-deploy:
    runs-on: nestandarta/git_python3.8_ubuntu
    
    # запускаются по шагам
    # name - название шага
    # в github actions будет выводиться название шага, и процессы
    steps:
      - name: Api 🛎️
        run: pytest tests/api/tests/accounts/

      - name: Deploy 🚀
        uses: JamesIves/github-pages-deploy-action@3.7.1
        with:
          GITHUB_TOKEN: ${{ secrets.QA_GURU_D }}
          BRANCH: gh-pages
          FOLDER: public
          CLEAN: true
